.env
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
dist/
build/
state.json
audit.jsonl
state_*.json
audit_*.jsonl

# your own settings: your address, whether sending is on, your labels.
# config.example.yaml is what ships.
config.yaml

# local login (hashed, but still private)
auth.json
# personal knowledge base and run bookkeeping
known_facts.txt
idempotency.json
config.yaml.bak
*.bak
usage.jsonl

# regenerated by "email-workflow demo"
demo_state.json
demo_audit.jsonl
demo_idempotency.json

# which version this copy was updated to
.update-state.json
